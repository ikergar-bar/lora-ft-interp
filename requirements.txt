# Pinned versions verified 2026-06-18 (Python 3.x, CPU-only torch for now).
# torch: reinstall with the CUDA wheel for your GPU before training:
#   uv pip install torch --index-url https://download.pytorch.org/whl/cu<VER>
# Critical: do NOT upgrade transformers or transformer-lens independently (compatibility).

torch==2.7.1
transformers==5.12.1
peft==0.19.1
datasets==5.0.0
accelerate==1.14.0
transformer-lens==3.4.0
scikit-learn==1.9.0
umap-learn==0.5.12
matplotlib==3.11.0
pyyaml==6.0.3

# Optional experiment tracking:
# wandb

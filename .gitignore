# Environment
.venv/
__pycache__/
*.pyc
*.pyo
.env

# Dataset (downloaded at runtime, not versioned)
data/

# Model weights (large binaries — commit adapters via DVC or external storage if needed)
artifacts/*.bin
artifacts/*.safetensors
artifacts/*.pt
artifacts/*.pth
artifacts/merged_model/

# results/ is intentionally tracked (figures, metrics, tables)

# Experiment tracking
wandb/

# Jupyter
.ipynb_checkpoints/
